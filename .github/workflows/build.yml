name: build

on:
  push:
    branches: [ main ]
  release:
    types: [created]
  workflow_dispatch:

permissions:
  contents: write

jobs:

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: setup python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: install dependencies
        run: pip install customtkinter websockets cryptography requests aiortc pyinstaller

      - name: build exe
        run: pyinstaller --onefile --windowed --name chat --hidden-import=requests --hidden-import=charset_normalizer --hidden-import=aiortc --collect-all=customtkinter --collect-all=aiortc --python-option u client.py

      - name: upload exe to release
        uses: softprops/action-gh-release@v2
        if: github.event_name == 'release'
        with:
          files: dist/chat.exe

      - name: upload exe as artifact
        uses: actions/upload-artifact@v4
        with:
          name: chat-windows
          path: dist/chat.exe

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: setup python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: install dependencies
        run: pip install customtkinter websockets cryptography requests aiortc pyinstaller

      - name: build binary
        run: pyinstaller --onefile --name chat --hidden-import=requests --hidden-import=charset_normalizer --hidden-import=aiortc --collect-all=customtkinter --collect-all=aiortc client.py

      - name: upload binary to release
        uses: softprops/action-gh-release@v2
        if: github.event_name == 'release'
        with:
          files: dist/chat

      - name: upload binary as artifact
        uses: actions/upload-artifact@v4
        with:
          name: chat-linux
          path: dist/chat
